# Raw extracted book text. This is copyrighted source material -- only the
# synthesised skill files under skills/ are meant to persist.
extraction/

# Vendored upstream repos, cloned for reference.
vendor/

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# The reference pack distilled from Wiley GAAP 2020. Synthesised rather than
# copied, but still a derivative of a copyrighted book, so it is not
# redistributed from this public repository. The router falls back to
# ~/.claude/skills/wiley-gaap when the pack is installed locally.
skills/wiley-gaap/

# Local scratch.
exports/
.venv311/

# Stray runtime log.
pandasai.log
